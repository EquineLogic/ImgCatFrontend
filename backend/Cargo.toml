[package]
name = "backend"
version = "0.1.0"
edition = "2024"

[dependencies]
aws-sdk-s3 = "1.127.0"
axum = "0.8.8"
sqlx = "0.8.6"
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.6", features = ["cors"] }

